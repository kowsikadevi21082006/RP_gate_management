export function page(id, button) { if (['leave','td','vehicle','outpass','night'].includes(id)) window.RPGate.resetForm(id); document.querySelectorAll('.page').forEach((element) => element.classList.remove('active')); document.getElementById(id)?.classList.add('active'); document.querySelectorAll('.nav button').forEach((element) => element.classList.remove('active')); button?.classList.add('active'); if (id === 'records') window.RPGate.renderRecords(); if (id === 'master') window.RPGate.refreshPeople(); if (id === 'search') window.RPGate.renderHistory(); if (id === 'home') window.RPGate.render(); }
export function setFilter(category) { setTimeout(() => { const filter = document.getElementById('categoryFilter'); if (filter) filter.value = category; window.RPGate.renderRecords(); }, 50); }
